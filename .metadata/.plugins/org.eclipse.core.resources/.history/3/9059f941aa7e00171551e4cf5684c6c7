#include <stdio.h>

int main()
{
	int sockfd,buf[256],portno;
	char mip[32];
	printf("Enter Multicast IP address to Send Data :");
	scanf("%s",mip);
	printf("Enter Multicast Port : ");
	scanf("%d",&portno);
}
